version: '3'

services:
  database:
    image: postgres:16-alpine
    container_name: project_cars_db
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    volumes:
      - project_cars_vol:/var/lib/postgresql/data
    networks:
      - project_cars_net
    ports:
      - 5434:5432
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 3

  redis:
    image: redis:7-alpine
    container_name: project_cars_redis
    volumes:
      - project_cars_redis:/data
    networks:
      - project_cars_net
    ports:
      - 6379:6379
    restart: always

  minio:
    image: minio/minio:latest
    container_name: project_cars_minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    volumes:
      - project_cars_minio:/data
    networks:
      - project_cars_net
    ports:
      - 9000:9000
      - 9001:9001
    restart: always

  backend:
    image: kreeteen/projectcars-webapp:1.0
    container_name: project_cars_backend
    env_file:
      - .env
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://database:5432/projectcars
      SPRING_DATASOURCE_USERNAME: ${DB_USER}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASS}
      REDIS_HOST: redis
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
      MINIO_BUCKET: projectcars
      MINIO_MIGRATE_LOCAL: "true"
      SERVER_ADDRESS: 0.0.0.0
    volumes:
      - ./uploads:/workspace/uploads:ro
    ports:
      - 8080:8080
    networks:
      - project_cars_net
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_started
      minio:
        condition: service_started
    restart: on-failure

volumes:
  project_cars_vol:
    name: project_cars_vol
  project_cars_redis:
    name: project_cars_redis
  project_cars_minio:
    name: project_cars_minio


networks:
  project_cars_net:
    name: project_cars_net